name: Build Custom Unbound APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout OpenWrt Core Code
        uses: actions/checkout@v4
        with:
          repository: 'openwrt/openwrt'
          ref: 'master'
          path: 'openwrt'

      - name: Install System Dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3-setuptools rsync swig unzip zlib1g-dev file wget

      - name: Set Up Package Feeds
        run: |
          cd openwrt
          ./scripts/feeds update -a
          ./scripts/feeds install unbound-daemon libunbound

      - name: Configure Target and Force Subnet Feature Natively
        run: |
          cd openwrt
          # Select your exact MediaTek hardware target architecture
          echo "CONFIG_TARGET_mediatek=y" > .config
          echo "CONFIG_TARGET_mediatek_filogic=y" >> .config
          echo "CONFIG_TARGET_mediatek_filogic_DEVICE_dlink_aquila-pro-ai-m30-a1=y" >> .config
          
          # Force compile Unbound components
          echo "CONFIG_PACKAGE_libunbound=y" >> .config
          echo "CONFIG_PACKAGE_unbound-daemon=y" >> .config
          
          # NATIVE COMPILER FLAG: This tells the package builder to link all subnetcache components automatically
          echo "CONFIG_UNBOUND_SUBNETCACHE=y" >> .config
          
          # Tell OpenWrt's manifest code to explicitly read the sub-feature flag rules
          sed -i 's/DEPENDS:=/DEPENDS:=+unbound-subnetcache /g' feeds/packages/net/unbound/Makefile
          
          make defconfig

      - name: Compile and Package Binaries
        run: |
          cd openwrt
          make tools/install -j$(nproc)
          make toolchain/install -j$(nproc)
          make package/feeds/packages/unbound/compile V=s
          make package/index

      - name: Upload Finished APK File
        uses: actions/upload-artifact@v4
        with:
          name: custom-unbound-apk
          path: openwrt/bin/**/*.apk
